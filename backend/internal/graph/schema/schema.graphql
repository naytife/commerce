scalar DateTime

interface Node {
  id: ID!
}
type Image {
  url: String!
  altText: String
}

type Query {
  node(id: ID!): Node
}
type Mutation {
  signInUser(input: SignInInput!): SignInUserPayload!
}
