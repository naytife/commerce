fragment category on CreateCategorySuccess {
	category {
		id
		slug
		title
		description
		children {
			id
			slug
			title
			description
			updatedAt
			createdAt
		}
		allowedAttributes {
			title
			dataType
		}
		updatedAt
		createdAt
	}
}
