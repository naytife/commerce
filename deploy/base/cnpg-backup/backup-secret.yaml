apiVersion: v1
kind: Secret
metadata:
  name: cnpg-backup-secret
  namespace: naytife
  labels:
    app.kubernetes.io/name: cnpg-backup-secret
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/component: cnpg-backup
    app.kubernetes.io/part-of: naytife-platform
type: Opaque
stringData:
  # For local: MinIO credentials (placeholder)
  ACCESS_KEY_ID: "minio-access-key"
  SECRET_ACCESS_KEY: "minio-secret-key"
  # For production: OCI Object Storage
  # These will be overridden by environment-specific patches
