apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: naytife
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: naytife-platform
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app: postgres
